/*
import React from "react";
import s from "./Pizza.module.css"
import PizzaCard from "./PizzaCard";


const PizzaTest = (props) => {

    return (

        <div className={s.pizza}>
            {!props.isReady
                ? 'Загрузка...'
                :
                props.pizzas && props.pizzas.map((p, i) => (
                    <PizzaCard key={i} {...p}/>
                    ))
            }

        </div>
    );
};
export default PizzaTest*/
